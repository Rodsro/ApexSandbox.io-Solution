public Integer fibonacci(Integer n) {
    
    Integer ultimo = 1;
    Integer atual = 1;
    Integer temporario;

    if (n == 1 || n == 2) {
        return atual = 1;
    } else{
        for (Integer i = 3; i <= n; i++) {
        temporario = atual;
        atual += ultimo;
        ultimo = temporario;
        }
        return atual;
    }
    
    
}
